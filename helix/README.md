# Baltic for Helix

Copy `baltic.toml` into your Helix themes directory:

```sh
mkdir -p ~/.config/helix/themes
cp baltic.toml ~/.config/helix/themes/baltic.toml
```

Set the theme in `~/.config/helix/config.toml`:

```toml
theme = "baltic"
```
