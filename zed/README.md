# Baltic Theme for Zed

## Local Install

Copy or symlink the theme file:

```sh
mkdir -p ~/.config/zed/themes
ln -sf "$PWD/themes/baltic.json" ~/.config/zed/themes/baltic.json
```

Then set:

```json
{
  "theme": {
    "mode": "dark",
    "dark": "Baltic"
  }
}
```

