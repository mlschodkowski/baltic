# Baltic for Ghostty

Copy `Baltic` into your Ghostty themes directory:

```sh
mkdir -p ~/.config/ghostty/themes
cp Baltic ~/.config/ghostty/themes/Baltic
```

Use it from your Ghostty config:

```ini
theme = Baltic
```
